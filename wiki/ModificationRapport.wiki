#summary Dès que dans le code vous modifiez quelque chose (rajout d'un attribut, d'une méthode public ou protected, ou une classe) vous devez le signifier ici !!
#labels Featured

= Mercredi 15 =

Server.Server : init(String fileConfig, IIndoor indoor)

Adapter.CompositeAdapter : parse(List xml)

Adapter.CompositeAdapter : constructor(file OWL)

Translator.N3 : suppression des sous classes Horizontal et Vertical (pb déplacé dans le xml avec getMetaInfo)